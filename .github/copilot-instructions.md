# GitHub Copilot Instructions

Use the repository-wide operating model in `../AGENTS.md`.

Also consult `.github/instructions/*.instructions.md` when their description or
`applyTo` pattern matches the current task.
