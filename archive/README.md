# Archived AI Workflow

This directory preserves an earlier QUDT TTL agent and skill for historical
reference. It is not active guidance and must not be loaded, invoked, copied,
or treated as authoritative by current workflows.

The former discovery entrypoints were renamed deliberately:

- `agents/qudt-ttl-curator.agent.archived.md`
- `skills/qudt-ttl-generation/SKILL.archived.md`
- `README_AI.archived.md`

Supporting references and scripts remain unchanged so the earlier attempt can
be studied in context. Current QUDT work is governed by the repository's root
`AGENTS.md` and the active skill under `.agents/skills/`.

Historical files may be consulted only when a user explicitly requests a
retrospective or comparison. They must not supply current ontology rules.